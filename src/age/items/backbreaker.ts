import { emoji } from '../../lib/utils/emoji'
import { Item } from '../item'

export const backBreaker = new Item({
    id: 'item__backBreaker',
    name: `BackBreaker Beer`,
    description: `Lager's Estate's premium beer`,
    emoji: "🍺",
    cost:200,
    type:"none",
    skills:[]
})
