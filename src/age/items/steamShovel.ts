import { emoji } from '../../lib/utils/emoji'
import { Item } from '../item'

export const steamShovel = new Item({
    id: 'item__steamShovel',
    name: `Steam Shovel`,
    description: 'a shovel that operates on steam',
    emoji: "",
    cost:300,
    type:"none",
    skills:[]
})
