import { emoji } from '../../lib/utils/emoji'
import { Item } from '../item'

export const slimeBlob = new Item({
    id: 'item__slimeBlob',
    name: 'Slime Blob',
    description: 'A blob of slime',
    emoji: emoji.SLIME_GOO,
})
