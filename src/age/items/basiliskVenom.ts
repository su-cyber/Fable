import { emoji } from '../../lib/utils/emoji'
import { Item } from '../item'

export const basiliskVenom = new Item({
    id: 'item__basiliskVenom',
    name: "Basilisk's Venom",
    description: 'Extremely potent Venom of the Basilisk',
    emoji: emoji.POISON,
})
