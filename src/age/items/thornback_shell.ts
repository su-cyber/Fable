import { Item } from '../item'

export const thornbackShell = new Item({
    id: 'item__thornbackShell',
    name: `Thornback Shell`,
    description: `the shell of a thornback`,
    emoji: "🐚",
    cost:800,
    type:"none",
    skills:[]
})