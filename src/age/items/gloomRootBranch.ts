import { emoji } from '../../lib/utils/emoji'
import { Item } from '../item'

export const gloomRoot_branch = new Item({
    id: 'item__gloomRootBranch',
    name: `Gloomroot Branch`,
    description: 'the branch of a Gloomroot',
    emoji: "🌴",
    cost:150,
    type:"none",
    skills:[]
})
